#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['solo', 'solo.cli', 'solo.fido2']

package_data = \
{'': ['*']}

install_requires = \
['click >= 7.0',
 'cryptography',
 'ecdsa',
 'fido2 ~= 0.8.1',
 'intelhex',
 'pyserial',
 'pyusb',
 'requests']

entry_points = \
{'console_scripts': ['solo = solo.cli:solo_cli']}

setup(name='onlykey-solo-python',
      version='0.0.27',
      description='Python library for OnlyKey with Solo FIDO2',
      author='CryptoTrust',
      author_email='admin@crp.to',
      url='https://github.com/trustcrypto/onlykey-solo-python',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3.6',
     )
