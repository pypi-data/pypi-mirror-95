[tool.poetry]
name = "deploy-monkey"
version = "0.0.1"
description = ""
authors = ["Tino de Bruijn <tino@solarmonkey.nl>"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]
twine = "^3.3.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
