[tool.poetry]
name = "portscanner"
version = "0.5.3"
description = "Asynchronous Port Scanner"
authors = ["GoodiesHQ <aarcher73k@gmail.com>"]

[tool.poetry.scripts]
portscanner = 'portscanner.console:run'

[tool.poetry.dependencies]
python = "^3.8"
aiodns = "^2.0.0"
colorama = "^0.4.4"

[tool.poetry.dev-dependencies]
pytest = "^6.2.2"
black = "^20.8b1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
