[metadata]
name = mei
version = 0.0.0

[aliases]
dists = sdist bdist_wheel

[options]
package_dir = 
	= src
packages = find_namespace:
python_requires = >=3.6
install_requires = 

[options.packages.find]
where = src
exclude = tests

[tool:pytest]
testpaths = 
	tests

[egg_info]
tag_build = 
tag_date = 0

