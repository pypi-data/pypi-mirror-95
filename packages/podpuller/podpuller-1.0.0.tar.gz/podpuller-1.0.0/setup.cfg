[metadata]
name = podpuller
version = 1.0.0

[options]
packages = podpuller
install_requires = 
	requests
	SQLObject
	tqdm
	bullet
	termcolor
	feedparser
python_requires = >= 3.7

[options.entry_points]
console_scripts = 
	podpuller = podpuller.podpuller:main

[egg_info]
tag_build = 
tag_date = 0

