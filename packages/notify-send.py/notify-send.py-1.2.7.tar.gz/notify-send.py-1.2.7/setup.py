#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['notify_send_py', 'notify_send_py.notify3']

package_data = \
{'': ['*']}

install_requires = \
['dbus-python', 'pygobject']

extras_require = \
{'dev': ['flit', 'pygments']}

entry_points = \
{'console_scripts': ['notify-send.py = notify_send_py.notify_send_py:main']}

setup(name='notify-send.py',
      version='1.2.7',
      description='Script and module for sending desktop notifications',
      author='Philipp Uhl',
      author_email='git@ph-uhl.com',
      url='https://github.com/phuhl/notify-send.py',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      entry_points=entry_points,
     )
