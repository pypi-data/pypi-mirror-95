[tool.poetry]
name = "dora-magic"
version = "0.1.1"
description = ""
authors = ["didone <didone@compasso.com.br>"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]
pytest = "^5.2"
pylint = "^2.6.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
