# coding: utf-8

import sys
import os
from setuptools import setup, find_packages

NAME = "sourav-pet-project"
#TRAVIS_BUILD_NUMBER is a built in env var in travis
#See travis env vars here https://docs.travis-ci.com/user/environment-variables/
VERSION = f"1.3.18b{os.environ.get('TRAVIS_BUILD_NUMBER', '0')}"
# To install the library, run the following
#
# python setup.py install
#
# prerequisite: setuptools
# http://pypi.python.org/pypi/setuptools

REQUIRES = ["connexion"]

setup(
    name=NAME,
    version=VERSION,
    description="Sokurav's pet prokjkect",
    author_email="",
    url="",
    keywords=["Swagger", "Swagger Petstore"],
    install_requires=REQUIRES,
    packages=find_packages(),
    package_data={'': ['swagger/swagger.yaml']},
    include_package_data=True,
    entry_points={
        'console_scripts': ['startpet=swagger_server.__main__:main']},
    long_description="""\
    No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)
    """
)
