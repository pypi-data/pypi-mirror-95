{{stage}} = KubernetesPodOperator(
               image='{{img}}',
               cmds=[
               {%for cmd in cmds %}
               '{{cmd}}',
               {%endfor%}
               ],
               namespace='{{namespace}}',
               name='airflow-{{stage}}',
               is_delete_operator_pod=False,
               in_cluster=True,
               task_id='{{stage}}',
               get_logs=True,
               dag=dag
           )