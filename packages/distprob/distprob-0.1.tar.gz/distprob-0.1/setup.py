from setuptools import setup

setup(name='distprob',
      version='0.1',
      description='Gaussian and Binomial distributions',
      author='irishwy_2',
      packages=['distprob'],
      zip_safe=False)
