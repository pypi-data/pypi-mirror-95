[tool.poetry]
name = "scoredb-sdk"
version = "0.1.3"
description = "Python SDK of the ScoreDB v3 API Server."
license = "GPL-3.0-or-later"
authors = ["JingBh <jingbohao@yeah.net>"]
readme = "README.md"
repository = "https://github.com/ScoreDB/sdk-python.git"
packages = [
    { include = "scoredb" }
]

[tool.poetry.dependencies]
python = "^3.7"
requests = "^2.25.1"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
