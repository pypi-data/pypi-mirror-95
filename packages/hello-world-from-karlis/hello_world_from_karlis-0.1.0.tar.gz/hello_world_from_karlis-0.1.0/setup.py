from setuptools import find_packages, setup

setup(
        name='hello_world_from_karlis',
        version='0.1.0',
        author='Karlis from LV',
        packages=find_packages(),
        include_package_data=True,
        description='Hello World form LV. My first pypi package'
        )
