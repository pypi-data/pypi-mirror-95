from raw_config import *
koji_profile = 'odcs_stg'
createrepo_deltas = False

{%- if compose["builds"] %}
pkgset_koji_builds = [
{%- for build in compose["builds"].split(" ") %}
    '{{ build }}',
{%- endfor %}
]
{%- endif %}
